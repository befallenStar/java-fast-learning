server:
    port: 8083
spring:
    application:
        name: patient-server
eureka:
    client:
        service-url:
            defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka
    instance:
        prefer-ip-address: true